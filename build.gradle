plugins {
    id 'application'
}

jar {
    manifest {
        attributes 'Main-Class': 'Main'
    }
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

application {
    mainClass = 'Main'
}

run {
    standardInput = System.in
}
